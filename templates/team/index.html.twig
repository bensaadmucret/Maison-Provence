{% extends 'base.html.twig' %}

{% block title %}Notre Équipe | Maison Provence{% endblock %}

{% block body %}
<div class="bg-gray-50 min-h-screen">
    <div class="container mx-auto px-4 py-16">
        <div class="text-center mb-16">
            <h1 class="text-5xl font-bold text-gray-900 mb-6">Notre Équipe</h1>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                Découvrez les visages passionnés derrière Maison Provence. Chacun de nos membres apporte son expertise et son amour pour les produits provençaux.
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
            {% for member in team_members %}
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform transition-all duration-300 hover:shadow-2xl hover:-translate-y-2 flex flex-col items-center p-8 text-center group">
                    <div class="mb-6 w-48 h-48">
                        {% if member.photo %}
                            <img src="{{ vich_uploader_asset(member, 'photoFile') }}" 
                                 alt="{{ member.fullName }}"
                                 class="w-full h-full object-cover rounded-full border-4 border-primary transition-transform duration-500 group-hover:scale-105">
                        {% else %}
                            <div class="w-full h-full bg-gray-100 rounded-full flex items-center justify-center border-4 border-gray-200">
                                <svg class="w-24 h-24 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                </svg>
                            </div>
                        {% endif %}
                    </div>
                    
                    <div class="flex flex-col items-center">
                        <h2 class="text-2xl font-bold text-gray-900 mb-2">{{ member.firstname }} {{ member.name }}</h2>
                        <p class="text-lg text-primary font-semibold mb-4">{{ member.role }}</p>
                        
                        {% if member.description %}
                            <div class="text-gray-600 mb-6 line-clamp-3 max-w-prose">
                                {{ member.description|raw }}
                            </div>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>

        <div class="text-center mt-16">
            <div class="max-w-3xl mx-auto">
                <h3 class="text-3xl font-bold text-gray-900 mb-6">Notre Philosophie</h3>
                <p class="text-lg text-gray-600 leading-relaxed">
                    Chez Maison Provence, nous croyons que chaque produit raconte une histoire. Notre équipe est passionnée par la préservation et la célébration des traditions provençales. 
                    Chaque membre apporte une expertise unique, un dévouement inébranlable à la qualité et un amour profond pour notre région.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
